{
  "workflow_id": "tb-admit-run",
  "name": "Clock: admit",
  "description": "What moves a submission off `testing`, in two walks with an admitting runner between them, because Soma runs no model. PREPARE reads the manifest the competitor uploaded and checks it against the hash they declared, checks the artifact is in the bucket, rebuilds the registration field by field, and queues one `admissions` row. A runner whose role is `admit` claims it through the gate, registers the model on its own node by reference and digest -- the node fetches and re-hashes the object, so a row that lies fails admission -- lets Orion admit it, plays it over the game's reference observations, deletes it, and reports what it found. DECIDE reads that report through admission_facts(), measures S' from this clock's own HEAD and the runner's bytes, picks the season's class, applies the platform's policy to the stats and writes one verdict. A baseline lands `disabled` rather than `verified`. The branch that matters is on `fault`: a competitor's mistake rejects the version; a report that decided nothing goes back to the queue with its attempt spent, so a submission that fails the same way on every runner expires TIMED_OUT; and a fault of this clock's releases the item untouched. The claim is the fence. Every verdict, and every expiry this run stamped with its token, is then told to the owner by a keyed statement that may fail without failing the walk.",
  "tags": ["pkg:soma"],
  "condition": true,
  "loop": { "counter": "i", "max": 64 },
  "tasks": [
    {
      "id": "when_token",
      "condition": { "==": [{ "var": "temp_data.i" }, 0] },
      "tasks": [
        {
          "id": "token",
          "name": "This run's claim token",
          "function": {
            "name": "map",
            "input": {
              "mappings": [{ "path": "temp_data.token", "logic": { "random": ["uuid"] } }]
            }
          }
        },
        {
          "id": "expire",
          "name": "Reject what has run out of attempts",
          "function": {
            "name": "db_write",
            "input": {
              "connector": "soma-db",
              "query": "UPDATE model_versions v SET status = 'rejected', reject_reason = 'TIMED_OUT', admit_started_at = NULL, admit_token = ($3)::uuid WHERE v.status = 'testing' AND (v.admit_started_at IS NULL OR v.admit_started_at < now() - (($2)::int * interval '1 second')) AND EXISTS (SELECT 1 FROM admissions a WHERE a.version_id = v.id AND a.report IS NULL AND a.attempts >= ($1)::int AND (a.lease_expires_at IS NULL OR a.lease_expires_at < now()))",
              "params": [
                { "var": "metadata.vars.admit_attempts_max" },
                { "var": "metadata.vars.admit_timeout_s" },
                { "var": "temp_data.token" }
              ],
              "output": "temp_data.expired"
            }
          }
        },
        {
          "id": "claim",
          "name": "Claim up to admit_batch submissions",
          "function": {
            "name": "db_write",
            "input": {
              "connector": "soma-db",
              "query": "UPDATE model_versions m SET admit_started_at = now(), admit_token = ($1)::uuid WHERE m.id IN (SELECT c.id FROM model_versions c WHERE c.status = 'testing' AND (c.admit_started_at IS NULL OR c.admit_started_at < now() - (($3)::int * interval '1 second')) AND NOT EXISTS (SELECT 1 FROM admissions a WHERE a.version_id = c.id AND a.report IS NULL) ORDER BY c.created_at LIMIT ($2)::int FOR UPDATE SKIP LOCKED)",
              "params": [
                { "var": "temp_data.token" },
                { "var": "metadata.vars.admit_batch" },
                { "var": "metadata.vars.admit_timeout_s" }
              ],
              "output": "temp_data.claimed"
            }
          }
        },
        {
          "id": "batch",
          "name": "Read what this run claimed",
          "function": {
            "name": "db_read",
            "input": {
              "connector": "soma-db",
              "query": "SELECT json_build_object( 'n', count(*), 'items', coalesce(json_agg(json_build_object( 'model_id', v.id, 'model', ($5)::text || v.id::text, 'weights_hash', v.weights_hash, 'manifest_hash', v.manifest_hash, 'artifact_key', v.artifact_key, 'manifest_key', regexp_replace(v.artifact_key, 'model\\.onnx$', 'manifest.json'), 'budget_ops', coalesce((se.rules -> 'graph' ->> 'adapter_ops_max')::bigint, (g.manifest -> 'budgets' ->> 'adapter_ops_max')::bigint), 'observations_n', CASE WHEN jsonb_typeof(g.reference_observations) = 'array' THEN jsonb_array_length(g.reference_observations) ELSE 0 END, 'opset_min', coalesce((se.rules -> 'graph' ->> 'opset_min')::int, ($2)::int), 'opset_max', coalesce((se.rules -> 'graph' ->> 'opset_max')::int, ($3)::int), 'op_allowlist', coalesce( (SELECT jsonb_agg(o) FROM jsonb_array_elements_text(($4)::jsonb) AS o WHERE o IN (SELECT jsonb_array_elements_text( se.rules -> 'graph' -> 'op_allowlist'))), ($4)::jsonb), 'params_max', (se.rules -> 'graph' ->> 'params_max')::bigint, 'infer_us_max', (se.rules -> 'graph' ->> 'infer_us_max')::bigint, 'rules_ok', se.id IS NOT NULL, 'job', (SELECT admission_facts(a) FROM admissions a WHERE a.version_id = v.id)) ORDER BY v.created_at), '[]'::json)) AS body FROM model_versions v JOIN games g ON g.id = v.game_id JOIN seasons se ON se.id = v.season_id WHERE v.admit_token = ($1)::uuid AND v.status = 'testing'",
              "params": [
                { "var": "temp_data.token" },
                { "var": "metadata.vars.opset_min" },
                { "var": "metadata.vars.opset_max" },
                { "var": "metadata.vars.op_allowlist" },
                { "var": "metadata.vars.model_prefix" }
              ],
              "output": "temp_data.batch"
            }
          }
        }
      ]
    },
    {
      "id": "notify_expired",
      "name": "Tell the owners what just timed out",
      "condition": {
        "and": [
          { "==": [{ "var": "temp_data.i" }, 0] },
          { ">": [{ "var": "temp_data.expired.rows_affected" }, 0] }
        ]
      },
      "continue_on_error": true,
      "function": {
        "name": "db_write",
        "input": {
          "connector": "soma-db",
          "query": "INSERT INTO notifications (user_id, category, kind, tone, subject, description, link, game, season, model_id, version_id, data, dedupe_key) SELECT e.owner_id, 'submissions', CASE v.status WHEN 'rejected' THEN 'alert' ELSE 'progress' END, CASE v.status WHEN 'verified' THEN 'info' WHEN 'active' THEN 'ok' ELSE 'bad' END, e.name || ' v' || v.version || CASE WHEN v.status = 'verified' THEN ' was admitted' WHEN v.status = 'active' THEN ' passed its trial' WHEN v.reject_reason = 'SEASON_CLOSED' THEN ' was withdrawn when its season closed' WHEN v.weight_class IS NULL THEN ' was rejected' ELSE ' failed its trial' END, CASE WHEN v.status = 'verified' THEN 'Admitted as ' || v.weight_class || ' at ' || to_char(v.size_bytes, 'FM999,999,999,990') || ' bytes. Its trial match is next.' WHEN v.status = 'active' THEN 'It is on the ' || v.weight_class || ' and open ladders.' ELSE v.reject_reason END, '/models/' || e.id || '/v' || v.version, g.slug, se.slug, e.id, v.id, jsonb_strip_nulls(jsonb_build_object( 'model', e.name, 'version', v.version, 'status', v.status, 'stage', CASE WHEN v.status = 'verified' THEN 'admission' WHEN v.status = 'active' THEN 'trial' WHEN v.reject_reason = 'SEASON_CLOSED' THEN 'season' WHEN v.weight_class IS NULL THEN 'admission' ELSE 'trial' END, 'class', v.weight_class, 'size_bytes', v.size_bytes, 'params', v.param_count, 'infer_us', v.infer_us, 'reason_code', v.reject_reason)), 'version:' || v.id || ':' || v.status FROM model_versions v JOIN models e ON e.id = v.model_id JOIN games g ON g.id = v.game_id JOIN seasons se ON se.id = v.season_id WHERE v.admit_token = ($1)::uuid AND v.reject_reason = 'TIMED_OUT' AND v.status IN ('verified', 'active', 'rejected') AND notification_wanted(e.owner_id, 'submissions') ON CONFLICT (user_id, dedupe_key) DO NOTHING",
          "params": [{ "var": "temp_data.token" }],
          "output": "temp_data.notified_expired"
        }
      }
    },
    {
      "id": "more",
      "name": "Stop when the work runs out",
      "function": {
        "name": "filter",
        "input": {
          "condition": { "<": [{ "var": "temp_data.i" }, { "var": "temp_data.batch.0.body.n" }] },
          "on_reject": "halt"
        }
      }
    },
    {
      "id": "item",
      "name": "Take submission i, and clear the last one",
      "function": {
        "name": "map",
        "input": {
          "mappings": [
            {
              "path": "temp_data.it",
              "logic": {
                "val": ["temp_data", "batch", 0, "body", "items", { "val": ["temp_data", "i"] }]
              }
            },
            { "path": "temp_data.head", "logic": false },
            { "path": "temp_data.man", "logic": false },
            { "path": "temp_data.mtext", "logic": false },
            { "path": "temp_data.signed", "logic": false },
            { "path": "temp_data.manok", "logic": false },
            { "path": "temp_data.reg", "logic": false },
            { "path": "temp_data.abytes", "logic": false },
            { "path": "temp_data.clsrow", "logic": false },
            { "path": "temp_data.cls", "logic": false },
            { "path": "temp_data.ops", "logic": false },
            { "path": "temp_data.reason", "logic": false },
            { "path": "temp_data.again", "logic": false },
            {
              "path": "temp_data.retry",
              "logic": {
                "if": [
                  { "var": "temp_data.it.job" },
                  false,
                  { "!": { "var": "temp_data.it.budget_ops" } },
                  "MANIFEST_INCOMPLETE",
                  { "!": { "var": "temp_data.it.observations_n" } },
                  "MANIFEST_INCOMPLETE",
                  { "!": { "var": "temp_data.it.artifact_key" } },
                  "MANIFEST_INCOMPLETE",
                  false
                ]
              }
            }
          ]
        }
      }
    },
    {
      "id": "head",
      "name": "Is the artifact in the bucket?",
      "condition": {
        "and": [{ "!": { "var": "temp_data.it.job" } }, { "!": { "var": "temp_data.retry" } }]
      },
      "continue_on_error": true,
      "function": {
        "name": "storage_head",
        "input": {
          "connector": "soma-models-internal",
          "key": { "var": "temp_data.it.artifact_key" },
          "output": "temp_data.head"
        }
      }
    },
    {
      "id": "sign",
      "name": "Sign a GET for the manifest",
      "condition": {
        "and": [
          { "!": { "var": "temp_data.it.job" } },
          { "!": { "var": "temp_data.retry" } },
          { "var": "temp_data.head.exists" }
        ]
      },
      "continue_on_error": true,
      "function": {
        "name": "storage_presign",
        "input": {
          "connector": "soma-models-internal",
          "method": "GET",
          "key": { "var": "temp_data.it.manifest_key" },
          "expires_in": "5m",
          "output": "temp_data.signed"
        }
      }
    },
    {
      "id": "fetch_text",
      "name": "The manifest, as the bytes that were uploaded",
      "condition": { "!!": { "var": "temp_data.signed" } },
      "continue_on_error": true,
      "function": {
        "name": "http_call",
        "input": {
          "connector": "soma-models-http",
          "method": "GET",
          "path": {
            "substr": [
              { "var": "temp_data.signed" },
              { "length": [{ "var": "metadata.vars.models_endpoint" }] }
            ]
          },
          "output": "temp_data.mtext",
          "response_format": "text"
        }
      }
    },
    {
      "id": "when_fetch",
      "condition": { "!!": { "var": "temp_data.mtext" } },
      "tasks": [
        {
          "id": "fetch",
          "name": "The manifest, parsed",
          "continue_on_error": true,
          "function": {
            "name": "http_call",
            "input": {
              "connector": "soma-models-http",
              "method": "GET",
              "path": {
                "substr": [
                  { "var": "temp_data.signed" },
                  { "length": [{ "var": "metadata.vars.models_endpoint" }] }
                ]
              },
              "output": "temp_data.man"
            }
          }
        },
        {
          "id": "manifest_ok",
          "name": "Does it hash to what was declared?",
          "function": {
            "name": "db_read",
            "input": {
              "connector": "soma-db",
              "query": "SELECT ('sha256:' || encode(sha256(convert_to(($2)::text, 'UTF8')), 'hex') = ($1)::text) AS ok, length(($2)::text) AS bytes",
              "params": [{ "var": "temp_data.it.manifest_hash" }, { "var": "temp_data.mtext" }],
              "output": "temp_data.manok"
            }
          }
        }
      ]
    },
    {
      "id": "shape",
      "name": "The registration, rebuilt field by field",
      "condition": { "===": [{ "var": "temp_data.manok.0.ok" }, true] },
      "function": {
        "name": "map",
        "input": {
          "mappings": [
            {
              "path": "temp_data.reg",
              "logic": {
                "abi": { "var": "temp_data.man.abi" },
                "name": { "var": "temp_data.it.model" },
                "version": { "??": [{ "var": "temp_data.man.version" }, "1"] },
                "format": { "??": [{ "var": "temp_data.man.format" }, "onnx"] },
                "description": { "??": [{ "var": "temp_data.man.description" }, ""] },
                "inputs": { "var": "temp_data.man.inputs" },
                "outputs": { "var": "temp_data.man.outputs" },
                "probe_dims": {
                  "??": [
                    { "var": "temp_data.man.probe_dims" },
                    {}
                  ]
                }
              }
            },
            {
              "path": "temp_data.abytes",
              "logic": { "??": [{ "var": "temp_data.head.size" }, 0] }
            },
            {
              "path": "temp_data.reason",
              "logic": {
                "if": [
                  { "var": "temp_data.reason" },
                  { "var": "temp_data.reason" },
                  { "!": { "var": "temp_data.man.inputs" } },
                  "MANIFEST_INVALID",
                  { "!": { "var": "temp_data.man.outputs" } },
                  "MANIFEST_INVALID",
                  null
                ]
              }
            }
          ]
        }
      }
    },
    {
      "id": "reject_result",
      "name": "A manifest may not decode its own head",
      "condition": {
        "and": [{ "!!": { "var": "temp_data.man" } }, { "!!": { "var": "temp_data.man.result" } }]
      },
      "function": {
        "name": "map",
        "input": { "mappings": [{ "path": "temp_data.reason", "logic": "RESULT_NOT_ALLOWED" }] }
      }
    },
    {
      "id": "sift",
      "name": "Can it be queued, and if not whose fault is it?",
      "condition": { "!": { "var": "temp_data.it.job" } },
      "function": {
        "name": "map",
        "input": {
          "mappings": [
            {
              "path": "temp_data.retry",
              "logic": {
                "if": [
                  { "var": "temp_data.retry" },
                  { "var": "temp_data.retry" },
                  { "!": { "var": "temp_data.head" } },
                  "STORE_UNREACHABLE",
                  null
                ]
              }
            },
            {
              "path": "temp_data.reason",
              "logic": {
                "if": [
                  { "var": "temp_data.reason" },
                  { "var": "temp_data.reason" },
                  { "var": "temp_data.retry" },
                  null,
                  { "===": [{ "var": "temp_data.head.exists" }, false] },
                  "ARTIFACT_MISSING",
                  { "!": { "var": "temp_data.man" } },
                  "MANIFEST_MISSING",
                  { "!==": [{ "var": "temp_data.manok.0.ok" }, true] },
                  "MANIFEST_MISMATCH",
                  null
                ]
              }
            }
          ]
        }
      }
    },
    {
      "id": "queue",
      "name": "Queue it for an admitting runner",
      "condition": {
        "and": [
          { "!": { "var": "temp_data.it.job" } },
          { "!!": { "var": "temp_data.reg" } },
          { "!": { "var": "temp_data.reason" } },
          { "!": { "var": "temp_data.retry" } }
        ]
      },
      "function": {
        "name": "db_write",
        "input": {
          "connector": "soma-db",
          "query": "INSERT INTO admissions (version_id, registration, manifest, artifact_bytes, budget_ops) SELECT v.id, ($2)::jsonb, ($3)::text, ($4)::bigint, ($5)::bigint FROM model_versions v WHERE v.id = ($1)::uuid AND v.status = 'testing' AND v.admit_token = ($6)::uuid ON CONFLICT (version_id) DO NOTHING",
          "params": [
            { "var": "temp_data.it.model_id" },
            { "var": "temp_data.reg" },
            { "var": "temp_data.mtext" },
            { "var": "temp_data.abytes" },
            { "var": "temp_data.it.budget_ops" },
            { "var": "temp_data.token" }
          ],
          "output": "temp_data.queued"
        }
      }
    },
    {
      "id": "sort",
      "name": "Did the report decide anything?",
      "condition": { "!!": { "var": "temp_data.it.job" } },
      "function": {
        "name": "map",
        "input": {
          "mappings": [
            {
              "path": "temp_data.again",
              "logic": {
                "if": [
                  { "var": "temp_data.it.job.again" },
                  { "var": "temp_data.it.job.again" },
                  { "!": { "===": [{ "var": "temp_data.it.job.admitted" }, true] } },
                  null,
                  { "!": { "var": "temp_data.it.job.probe" } },
                  "PROBE_UNREACHABLE",
                  { "===": [{ "var": "temp_data.it.job.opset" }, null] },
                  "REPORT_INCOMPLETE",
                  { "===": [{ "var": "temp_data.it.job.parameters" }, null] },
                  "REPORT_INCOMPLETE",
                  null
                ]
              }
            },
            {
              "path": "temp_data.retry",
              "logic": {
                "if": [{ "!": { "var": "temp_data.it.rules_ok" } }, "SEASON_RULES_INCOMPLETE", null]
              }
            }
          ]
        }
      }
    },
    {
      "id": "classify",
      "name": "Which class does it measure into",
      "condition": {
        "and": [
          { "!!": { "var": "temp_data.it.job" } },
          { "===": [{ "var": "temp_data.it.job.admitted" }, true] }
        ]
      },
      "function": {
        "name": "db_read",
        "input": {
          "connector": "soma-db",
          "query": "SELECT (SELECT e ->> 'class' FROM seasons s, jsonb_array_elements(s.weight_classes) AS e WHERE s.id = v.season_id AND (e ->> 'max_bytes')::bigint >= ($2)::bigint AND season_admits_class(s, (e ->> 'class')::ladder) ORDER BY (e ->> 'max_bytes')::bigint LIMIT 1) AS cls, (SELECT count(*) > 0 FROM seasons s, jsonb_array_elements(s.weight_classes) AS e WHERE s.id = v.season_id AND (e ->> 'max_bytes')::bigint >= ($2)::bigint) AS fits_any FROM model_versions v WHERE v.id = ($1)::uuid",
          "params": [{ "var": "temp_data.it.model_id" }, { "var": "temp_data.it.job.size" }],
          "output": "temp_data.clsrow"
        }
      }
    },
    {
      "id": "judge",
      "name": "Class, opset, operators and the budget",
      "condition": { "!!": { "var": "temp_data.it.job" } },
      "function": {
        "name": "map",
        "input": {
          "mappings": [
            { "path": "temp_data.cls", "logic": { "var": "temp_data.clsrow.0.cls" } },
            {
              "path": "temp_data.ops",
              "logic": {
                "reduce": [
                  { "var": "temp_data.it.job.operators" },
                  {
                    "allow": { "var": "accumulator.allow" },
                    "bad": {
                      "if": [
                        { "in": [{ "var": "current" }, { "var": "accumulator.allow" }] },
                        { "var": "accumulator.bad" },
                        {
                          "merge": [
                            { "var": "accumulator.bad" },
                            [{ "var": "current" }]
                          ]
                        }
                      ]
                    }
                  },
                  { "allow": { "var": "temp_data.it.op_allowlist" }, "bad": [] }
                ]
              }
            },
            {
              "path": "temp_data.reason",
              "logic": {
                "if": [
                  { "var": "temp_data.again" },
                  null,
                  { "var": "temp_data.retry" },
                  null,
                  { "var": "temp_data.it.job.refused" },
                  { "var": "temp_data.it.job.refused" },
                  { "!": { "===": [{ "var": "temp_data.it.job.admitted" }, true] } },
                  null,
                  {
                    "and": [
                      { "!": { "var": "temp_data.cls" } },
                      { "var": "temp_data.clsrow.0.fits_any" }
                    ]
                  },
                  "CLASS_NOT_OFFERED",
                  { "!": { "var": "temp_data.cls" } },
                  "TOO_LARGE",
                  {
                    "<": [{ "var": "temp_data.it.job.opset" }, { "var": "temp_data.it.opset_min" }]
                  },
                  "OPSET_UNSUPPORTED",
                  {
                    ">": [{ "var": "temp_data.it.job.opset" }, { "var": "temp_data.it.opset_max" }]
                  },
                  "OPSET_UNSUPPORTED",
                  { ">": [{ "length": [{ "var": "temp_data.ops.bad" }] }, 0] },
                  "OP_NOT_ALLOWED",
                  {
                    "and": [
                      { "!!": { "var": "temp_data.it.params_max" } },
                      {
                        ">": [
                          { "var": "temp_data.it.job.parameters" },
                          { "var": "temp_data.it.params_max" }
                        ]
                      }
                    ]
                  },
                  "PARAMS_EXCEEDED",
                  {
                    "and": [
                      { "!": { "var": "temp_data.it.job.probe.ok" } },
                      { "var": "temp_data.it.job.probe.over_budget" }
                    ]
                  },
                  "ADAPTER_OVER_BUDGET",
                  { "!": { "var": "temp_data.it.job.probe.ok" } },
                  { "??": [{ "var": "temp_data.it.job.probe.reason" }, "ADAPTER_INVALID"] },
                  {
                    "and": [
                      { "!!": { "var": "temp_data.it.infer_us_max" } },
                      {
                        ">": [
                          { "var": "temp_data.it.job.probe.infer_us_max" },
                          { "var": "temp_data.it.infer_us_max" }
                        ]
                      }
                    ]
                  },
                  "TOO_SLOW",
                  null
                ]
              }
            }
          ]
        }
      }
    },
    {
      "id": "verify",
      "name": "testing -> verified (a baseline: disabled)",
      "condition": {
        "and": [
          { "!!": { "var": "temp_data.it.job" } },
          { "===": [{ "var": "temp_data.it.job.admitted" }, true] },
          { "!!": { "var": "temp_data.cls" } },
          { "!": { "var": "temp_data.reason" } },
          { "!": { "var": "temp_data.again" } },
          { "!": { "var": "temp_data.retry" } }
        ]
      },
      "function": {
        "name": "db_write",
        "input": {
          "connector": "soma-db",
          "query": "UPDATE model_versions SET status = CASE WHEN EXISTS (SELECT 1 FROM models e JOIN users u ON u.id = e.owner_id WHERE e.id = model_versions.model_id AND u.role = 'baseline') THEN 'disabled'::model_status ELSE 'verified'::model_status END, weight_class = ($2)::ladder, size_bytes = ($3)::bigint, param_count = ($4)::bigint, infer_us = ($5)::float8::bigint, manifest = (SELECT a.manifest FROM admissions a WHERE a.version_id = model_versions.id), orion_version = ($6)::text, probe_dims = ($8)::jsonb, admit_started_at = NULL, reject_reason = NULL WHERE id = ($1)::uuid AND status = 'testing' AND admit_token = ($7)::uuid AND EXISTS (SELECT 1 FROM admissions a WHERE a.version_id = model_versions.id)",
          "params": [
            { "var": "temp_data.it.model_id" },
            { "var": "temp_data.cls" },
            { "var": "temp_data.it.job.size" },
            { "var": "temp_data.it.job.parameters" },
            { "var": "temp_data.it.job.probe.infer_us_max" },
            { "var": "metadata.vars.orion_version" },
            { "var": "temp_data.token" },
            { "var": "temp_data.it.job.probe_dims" }
          ],
          "output": "temp_data.verified"
        }
      }
    },
    {
      "id": "reject",
      "name": "Reject, with the word a competitor reads",
      "condition": { "!!": { "var": "temp_data.reason" } },
      "function": {
        "name": "db_write",
        "input": {
          "connector": "soma-db",
          "query": "UPDATE model_versions SET status = 'rejected', reject_reason = ($2)::text, admit_started_at = NULL WHERE id = ($1)::uuid AND status = 'testing' AND admit_token = ($3)::uuid",
          "params": [
            { "var": "temp_data.it.model_id" },
            { "var": "temp_data.reason" },
            { "var": "temp_data.token" }
          ],
          "output": "temp_data.rejected"
        }
      }
    },
    {
      "id": "requeue",
      "name": "Send the report back to the queue",
      "condition": {
        "and": [{ "!!": { "var": "temp_data.it.job" } }, { "!!": { "var": "temp_data.again" } }]
      },
      "function": {
        "name": "db_write",
        "input": {
          "connector": "soma-db",
          "query": "UPDATE admissions a SET report = NULL, reported_at = NULL, claim_token = NULL, lease_expires_at = NULL, requeued_for = ($3)::text WHERE a.version_id = ($1)::uuid AND a.report IS NOT NULL AND EXISTS (SELECT 1 FROM model_versions v WHERE v.id = a.version_id AND v.status = 'testing' AND v.admit_token = ($2)::uuid)",
          "params": [
            { "var": "temp_data.it.model_id" },
            { "var": "temp_data.token" },
            { "var": "temp_data.again" }
          ],
          "output": "temp_data.requeued"
        }
      }
    },
    {
      "id": "release",
      "name": "Release the clock's claim",
      "function": {
        "name": "db_write",
        "input": {
          "connector": "soma-db",
          "query": "UPDATE model_versions SET admit_started_at = NULL, admit_token = NULL WHERE id = ($1)::uuid AND status = 'testing' AND admit_token = ($2)::uuid",
          "params": [{ "var": "temp_data.it.model_id" }, { "var": "temp_data.token" }],
          "output": "temp_data.released"
        }
      }
    },
    {
      "id": "notify",
      "name": "Tell the owner the verdict",
      "condition": {
        "and": [
          { "!": { "var": "temp_data.retry" } },
          { "!": { "var": "temp_data.again" } },
          {
            "or": [{ "!!": { "var": "temp_data.it.job" } }, { "!!": { "var": "temp_data.reason" } }]
          }
        ]
      },
      "continue_on_error": true,
      "function": {
        "name": "db_write",
        "input": {
          "connector": "soma-db",
          "query": "INSERT INTO notifications (user_id, category, kind, tone, subject, description, link, game, season, model_id, version_id, data, dedupe_key) SELECT e.owner_id, 'submissions', CASE v.status WHEN 'rejected' THEN 'alert' ELSE 'progress' END, CASE v.status WHEN 'verified' THEN 'info' WHEN 'active' THEN 'ok' ELSE 'bad' END, e.name || ' v' || v.version || CASE WHEN v.status = 'verified' THEN ' was admitted' WHEN v.status = 'active' THEN ' passed its trial' WHEN v.reject_reason = 'SEASON_CLOSED' THEN ' was withdrawn when its season closed' WHEN v.weight_class IS NULL THEN ' was rejected' ELSE ' failed its trial' END, CASE WHEN v.status = 'verified' THEN 'Admitted as ' || v.weight_class || ' at ' || to_char(v.size_bytes, 'FM999,999,999,990') || ' bytes. Its trial match is next.' WHEN v.status = 'active' THEN 'It is on the ' || v.weight_class || ' and open ladders.' ELSE v.reject_reason END, '/models/' || e.id || '/v' || v.version, g.slug, se.slug, e.id, v.id, jsonb_strip_nulls(jsonb_build_object( 'model', e.name, 'version', v.version, 'status', v.status, 'stage', CASE WHEN v.status = 'verified' THEN 'admission' WHEN v.status = 'active' THEN 'trial' WHEN v.reject_reason = 'SEASON_CLOSED' THEN 'season' WHEN v.weight_class IS NULL THEN 'admission' ELSE 'trial' END, 'class', v.weight_class, 'size_bytes', v.size_bytes, 'params', v.param_count, 'infer_us', v.infer_us, 'reason_code', v.reject_reason)), 'version:' || v.id || ':' || v.status FROM model_versions v JOIN models e ON e.id = v.model_id JOIN games g ON g.id = v.game_id JOIN seasons se ON se.id = v.season_id WHERE v.id = ($1)::uuid AND v.status IN ('verified', 'active', 'rejected') AND notification_wanted(e.owner_id, 'submissions') ON CONFLICT (user_id, dedupe_key) DO NOTHING",
          "params": [{ "var": "temp_data.it.model_id" }],
          "output": "temp_data.notified"
        }
      }
    }
  ]
}
