{
  "workflow_id": "soma-models-get",
  "name": "One model",
  "description": "One MODEL -- an entry -- and every version of it, addressed by its id. It used to be addressed by the GitHub repository it was published from, on the argument that the repository WAS the key and the permalink was therefore readable and constructible from a GitHub URL. The repository left the submission path because it limited nothing and cost a great deal, so the key is the id, and this route takes the shape /v1/matches/{id} and /v1/versions/{id} already use. The entry's `name` stays free text and display-only, which is what lets it stay free text: it never has to survive a URL. Public, like the version permalink under it, because a ladder nobody can audit is not a ladder. Every version is returned, newest first, with its own ratings: the entry is the lineage and the versions are its history, which is the shape the Model screen prints and the reason this route exists at all -- before the split there was no row to hang it on.",
  "tags": ["pkg:soma"],
  "tasks": [
    {
      "id": "query",
      "name": "Query",
      "function": {
        "name": "db_read",
        "input": {
          "connector": "soma-db",
          "query": "SELECT json_build_object( 'model_id', e.id, 'model', e.name, 'owner', e.owner_id, 'owner_handle', u.handle, 'baseline', u.role = 'baseline', 'game', g.slug, 'created_at', e.created_at, 'retired', e.retired_at IS NOT NULL, 'retired_at', e.retired_at, 'versions', coalesce((SELECT json_agg(json_build_object( 'version_id', v.id, 'version', v.version, 'class', v.weight_class, 'size_bytes', v.size_bytes, 'param_count', v.param_count, 'infer_us', v.infer_us, 'status', v.status, 'phase', model_phase(v), 'reject_reason', v.reject_reason, 'created_at', v.created_at, 'weights_hash', v.weights_hash, 'manifest_hash', v.manifest_hash, 'season', (SELECT se.slug FROM seasons se WHERE se.id = v.season_id), 'ratings', model_ratings(v.id, ($2)::float8), 'last_played_at', (SELECT max(mt.played_at) FROM match_seats ms JOIN matches mt ON mt.id = ms.match_id WHERE ms.version_id = v.id)) ORDER BY v.version DESC) FROM model_versions v WHERE v.model_id = e.id), '[]'::json)) AS body FROM models e JOIN users u ON u.id = e.owner_id JOIN games g ON g.id = e.game_id WHERE e.id = ($1)::uuid",
          "params": [{ "var": "metadata.params.id" }, { "var": "metadata.vars.settled_sigma" }],
          "output": "temp_data.rows"
        }
      }
    },
    {
      "id": "respond",
      "name": "Respond",
      "function": {
        "name": "map",
        "input": {
          "mappings": [
            { "path": "data.body", "logic": { "var": "temp_data.rows.0.body" } },
            { "path": "data._orion.response", "logic": { "$from": "constants.ok" } }
          ]
        }
      }
    }
  ]
}
