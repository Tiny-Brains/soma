{
  "channel_id": "soma-gate-roster",
  "name": "soma-gate-roster",
  "description": "What the ladder says this node should be able to play. A replica's own tb-roster clock reads it; no clock ever calls a replica.",
  "tags": ["soma", "gate", "models"],
  "channel_type": "sync",
  "protocol": "rest",
  "methods": ["GET"],
  "route_pattern": "/v1/runner/roster",
  "workflow_id": "soma-gate-roster",
  "config": {
    "response": { "$from": "constants.shaped" },
    "timeout_ms": 10000,
    "rate_limit": { "$from": "constants.runner_rate" },
    "auth": { "$from": "constants.runner_auth" },
    "principal_rate_limit": { "$from": "constants.per_runner_rate" }
  }
}
