{
  "channel_id": "soma-admin-check",
  "name": "soma-admin-check",
  "description": "Whether this session is an admin. THE ONE SESSION ROUTE WITH NO RATE LIMIT, deliberately: web's nginx calls it through auth_request on every Orion console request, so limiting it would lock an admin out of the console.",
  "tags": ["soma", "admin", "auth"],
  "channel_type": "sync",
  "protocol": "rest",
  "methods": ["GET"],
  "route_pattern": "/v1/admin-check",
  "workflow_id": "soma-admin-check",
  "config": {
    "response": { "$from": "constants.shaped" },
    "timeout_ms": 5000,
    "auth": { "$from": "constants.session_auth" },
    "principal_rate_limit": {
      "requests_per_second": 100,
      "burst": 200,
      "key_logic": { "var": "auth.sub" }
    }
  }
}
