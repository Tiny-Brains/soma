{
  "channel_id": "soma-clock-reap",
  "name": "soma-clock-reap",
  "description": "Returns lapsed leases to the queue. Every 5 seconds, the fastest clock, because a crashed runner's rows are unplayable until it runs. Three lapses fail the row.",
  "tags": ["soma", "clock", "matches"],
  "channel_type": "async",
  "protocol": "cron",
  "workflow_id": "soma-clock-reap-run",
  "transport_config": {
    "schedule": "*/5 * * * * *",
    "timezone": "UTC",
    "misfire_policy": "latest",
    "concurrency": { "policy": "forbid", "key": "runner-reap" }
  },
  "config": { "timeout_ms": 10000, "tracing": { "$from": "constants.clock_tracing" } }
}
