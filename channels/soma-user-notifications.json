{
  "channel_id": "soma-user-notifications",
  "name": "soma-user-notifications",
  "description": "The bell. Uncached and per-session; web polls it, which is why the response is shaped rather than raw.",
  "tags": ["soma", "user", "notifications"],
  "channel_type": "sync",
  "protocol": "rest",
  "methods": ["GET"],
  "route_pattern": "/v1/me/notifications",
  "workflow_id": "soma-user-notifications",
  "config": {
    "response": { "$from": "constants.shaped" },
    "timeout_ms": 10000,
    "rate_limit": { "$from": "constants.session_rate" },
    "auth": { "$from": "constants.session_auth" },
    "principal_rate_limit": { "$from": "constants.per_user_read_rate" }
  }
}
