{
  "channel_id": "soma-clock-pair",
  "name": "soma-clock-pair",
  "description": "Reads demand and inserts queued matches and their seats, pinning each one's board. Its demand statement is also scripts/autoscaler.sql's, which check-sql.sh diffs.",
  "tags": ["soma", "clock", "matches"],
  "channel_type": "async",
  "protocol": "cron",
  "workflow_id": "soma-clock-pair-run",
  "transport_config": {
    "schedule": "*/15 * * * * *",
    "timezone": "UTC",
    "misfire_policy": "latest",
    "concurrency": { "policy": "forbid", "key": "pair" }
  },
  "config": { "timeout_ms": 60000, "tracing": { "$from": "constants.clock_tracing" } }
}
