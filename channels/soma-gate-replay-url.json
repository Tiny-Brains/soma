{
  "channel_id": "soma-gate-replay-url",
  "name": "soma-gate-replay-url",
  "description": "Signs the replay PUT for RUNNER_BLOB_ENDPOINT -- the host a RUNNER dials, which is a third address for the one bucket -- and answers the endpoint alongside the URL so the runner can subtract it.",
  "tags": ["soma", "gate", "matches"],
  "channel_type": "sync",
  "protocol": "rest",
  "methods": ["POST"],
  "route_pattern": "/v1/runner/matches/{id}/replay-url",
  "workflow_id": "soma-gate-replay-url",
  "config": {
    "response": { "$from": "constants.shaped" },
    "timeout_ms": 10000,
    "rate_limit": { "$from": "constants.runner_rate" },
    "auth": { "$from": "constants.runner_auth" },
    "principal_rate_limit": { "$from": "constants.per_runner_rate" }
  }
}
