{
  "channel_id": "soma-pub-leaderboard",
  "name": "soma-pub-leaderboard",
  "description": "The standings, and the hottest read on the site. hot_cache, because every rated match moves it and a stale rank for a few seconds is cheaper than the query.",
  "tags": ["soma", "pub", "ladder"],
  "channel_type": "sync",
  "protocol": "rest",
  "methods": ["GET"],
  "route_pattern": "/v1/games/{game}/leaderboard",
  "workflow_id": "soma-pub-leaderboard",
  "config": {
    "response": { "$from": "constants.shaped" },
    "timeout_ms": 10000,
    "rate_limit": { "$from": "constants.public_rate" },
    "cache": { "$from": "constants.hot_cache" }
  }
}
