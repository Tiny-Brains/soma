{
  "channel_id": "soma-admin-baselines-list",
  "name": "soma-admin-baselines-list",
  "tags": ["soma", "admin", "baselines"],
  "channel_type": "sync",
  "protocol": "rest",
  "methods": ["GET"],
  "route_pattern": "/v1/games/{game}/seasons/{slug}/baselines",
  "workflow_id": "soma-admin-baselines-list",
  "config": {
    "response": { "$from": "constants.shaped" },
    "timeout_ms": 10000,
    "rate_limit": { "$from": "constants.session_rate" },
    "auth": { "$from": "constants.session_auth" },
    "principal_rate_limit": { "$from": "constants.per_user_read_rate" }
  }
}
