{
  "channel_id": "soma-gate-admissions-report",
  "name": "soma-gate-admissions-report",
  "description": "The admitting runner reports facts and Soma decides. admission_facts() types what it sends; a renamed key here is a fact read as missing, and the report goes back to the queue until the submission expires.",
  "tags": ["soma", "gate", "admission"],
  "channel_type": "sync",
  "protocol": "rest",
  "methods": ["POST"],
  "route_pattern": "/v1/runner/admissions/{id}/report",
  "workflow_id": "soma-gate-admissions-report",
  "config": {
    "response": { "$from": "constants.shaped" },
    "timeout_ms": 10000,
    "rate_limit": { "$from": "constants.runner_rate" },
    "auth": { "$from": "constants.runner_auth" },
    "principal_rate_limit": { "$from": "constants.per_runner_rate" }
  }
}
