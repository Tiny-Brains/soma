{
  "channel_id": "soma-admin-maps-update",
  "name": "soma-admin-maps-update",
  "tags": ["soma", "admin", "maps"],
  "channel_type": "sync",
  "protocol": "rest",
  "methods": ["PATCH"],
  "route_pattern": "/v1/games/{game}/seasons/{slug}/maps/{map_id}",
  "workflow_id": "soma-admin-maps-update",
  "config": {
    "response": { "$from": "constants.shaped" },
    "timeout_ms": 20000,
    "rate_limit": { "$from": "constants.session_rate" },
    "auth": { "$from": "constants.session_auth" },
    "principal_rate_limit": { "$from": "constants.per_admin_board_rate" }
  }
}
