{
  "channel_id": "soma-clock-count",
  "name": "soma-clock-count",
  "tags": ["soma", "clock", "ladder"],
  "channel_type": "async",
  "protocol": "cron",
  "workflow_id": "soma-clock-count-run",
  "transport_config": {
    "schedule": "*/10 * * * * *",
    "timezone": "UTC",
    "misfire_policy": "latest",
    "concurrency": { "policy": "forbid", "key": "count" }
  },
  "config": { "timeout_ms": 60000, "tracing": { "$from": "constants.clock_tracing" } }
}
