{
  "channel_id": "soma-clock-count",
  "name": "soma-clock-count",
  "description": "Folds finished matches into ratings and promotes or rejects a version on its trial. The only writer of `ratings`.",
  "tags": ["soma", "clock", "ladder"],
  "channel_type": "async",
  "protocol": "cron",
  "workflow_id": "soma-clock-count-run",
  "transport_config": {
    "schedule": "*/10 * * * * *",
    "timezone": "UTC",
    "misfire_policy": "latest",
    "concurrency": { "policy": "forbid", "key": "count" }
  },
  "config": { "timeout_ms": 60000, "tracing": { "$from": "constants.loop_clock_tracing" } }
}
