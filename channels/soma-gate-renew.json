{
  "channel_id": "soma-gate-renew",
  "name": "soma-gate-renew",
  "description": "Extends the lease mid-match. Answers `applied`; false means the claim is gone and the run must halt.",
  "tags": ["soma", "gate", "matches"],
  "channel_type": "sync",
  "protocol": "rest",
  "methods": ["POST"],
  "route_pattern": "/v1/runner/matches/{id}/renew",
  "workflow_id": "soma-gate-renew",
  "config": {
    "response": { "$from": "constants.shaped" },
    "timeout_ms": 10000,
    "rate_limit": { "$from": "constants.runner_rate" },
    "auth": { "$from": "constants.runner_auth" },
    "principal_rate_limit": { "$from": "constants.per_runner_rate" },
    "tracing": { "$from": "constants.gate_tracing" }
  }
}
