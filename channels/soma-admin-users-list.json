{
  "channel_id": "soma-admin-users-list",
  "name": "soma-admin-users-list",
  "description": "Every user and their role.",
  "tags": ["soma", "admin", "users"],
  "channel_type": "sync",
  "protocol": "rest",
  "methods": ["GET"],
  "route_pattern": "/v1/admin/users",
  "workflow_id": "soma-admin-users-list",
  "config": {
    "response": { "$from": "constants.shaped" },
    "timeout_ms": 10000,
    "rate_limit": { "$from": "constants.session_rate" },
    "auth": { "$from": "constants.session_auth" },
    "principal_rate_limit": { "$from": "constants.per_user_read_rate" }
  }
}
